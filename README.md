# orderforge

A tiny order-processing app. It is written in plain JS/ESM, so it runs with no
install:

```
node --test      # run the tests
node index.js    # place a sample order (run from the project root)
```

Paths are resolved relative to the working directory, so run commands from the
project root.
